// Bump this version to indicate a change in Settings
// This is really just used for prod caching issues
// Where we need to know if the dev version is the version running in prod

const VERSION = "v3"

export default VERSION
